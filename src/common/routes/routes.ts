export const enum Routes {
  base = "/",

  //   auth
  auth = "/auth",
  login = "/auth/login",
  register = "/auth/register",

  // dashboard
  dashboard = "/app",
  home = "/app/dashboard",
  order = "/app/order",
  account = "/app/accounts",
  help = "/app/help",
  menu = "/app/menu",
  payment = "/app/payment",
  review = "/app/review",
  setting = "/app/setting",
}
